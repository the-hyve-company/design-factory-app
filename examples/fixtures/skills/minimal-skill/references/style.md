# style.md

Reference doc bundled inside the Minimal Smoke Skill fixture. Its
purpose is to prove the skill importer preserves the
`references/` subfolder verbatim instead of flattening it into the
SKILL.md body.

The content here is irrelevant to the test. The file's _presence_
under `skills/minimal-skill/references/style.md` after import is the
assertion.
